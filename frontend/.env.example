# Optional: pre-bake your Anthropic API key so you don't have to enter it in the UI.
# Copy this file to .env and fill in your key.
# The UI key-entry field always takes precedence over this value.
VITE_ANTHROPIC_API_KEY=sk-ant-your-key-here
